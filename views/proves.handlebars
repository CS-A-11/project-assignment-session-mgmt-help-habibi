 
<section>
    <div class="container">
    	{{!-- <div class="row"> --}}
     	     
    		      {{!-- {{#each proofs}}

    		<!--Profile Card 5-->
    		<div class="col-md-4 mt-4">
    		    <div class="card profile-card-5">
    		        <div class="card-img-block">
    		            <img class="card-img-top" src="https://images.unsplash.com/photo-1517832207067-4db24a2ae47c" height="200"width="200"alt="Card image cap">
    		        </div>
                    <div class="card-body pt-0">
                    <h5 class="card-title">{{title}}</h5>
                    <p class="card-text">{{details}}</p>
                    {{#files}}
                                    <a class="btn btn-primary" href="{{this}}">Download
                                        <hr>
                                    </a>{{/files}}
                   </div>
                </div>
     		</div>
             {{/each}} --}}
              {{#each proofs}}
            <div class="col-md-4">
                <div class="card-box">
                    <div class="card-title">
                        <h2>{{title}}</h2>
                        <h6>{{subject}}</h6>
                        <p>{{details}} </p>
                    </div>
                    <div class="card-link">
                           {{#files}}
                        <a  href="{{this}}" class="c-link">Download
                            <i class="fa fa-angle-right"></i>
                        </a>
                        {{/files}}
                    </div>
                </div>
            </div>
              {{/each}}
            
             
    		
    	</div>
    {{!-- </div> --}}
</section>

